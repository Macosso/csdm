utils::globalVariables(
  c(
    # cce_mean_group
    "beta_bar", "beta_se", "beta_z", "beta_p",
    # dynamic_cce_mg and friends (dplyr/tidyr NSE)
    ".unit_id", "results",
    "estimate", "std.error", "z", "p.value",
    # cd tests
    "cd_star"
  )
)
